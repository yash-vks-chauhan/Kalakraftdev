{
  "name": "artcommerce-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd artcommerce && npm run dev",
    "build": "cd artcommerce && npm install && npm run build",
    "start": "cd artcommerce && npm run start",
    "install": "cd artcommerce && npm install",
    "postinstall": "cd artcommerce && npx prisma generate"
  }
} 