'use client'

import NotificationContainer from './NotificationContainer'

export default function UserNotifications() {
  // This component shows user notifications for ALL users (not just admins)
  return <NotificationContainer />
}
